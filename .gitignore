# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# auto-generated at deploy (scripts/stamp-build.js) — never committed so the
# live badge hash always equals the latest committed HEAD (deploy makes no
# trailing commit). TEMP: remove with the build-badge feature.
lib/buildInfo.ts

app-example

# generated native folders
/ios
/android
firestore-debug.log
